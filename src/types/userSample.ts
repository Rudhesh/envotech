// models/User.ts
export interface UserSample {
    id: string; // Assuming the roles are strings, you can adjust this based on the actual role data structure
    name: string;
    email: string;
  }
  